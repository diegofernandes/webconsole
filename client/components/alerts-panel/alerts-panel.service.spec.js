'use strict';

describe('Service: alertsPanel', function () {

  // load the service's module
  beforeEach(module('meccanoAdminApp'));

  // instantiate service
  var alertsPanel;
  beforeEach(inject(function (_alertsPanel_) {
    alertsPanel = _alertsPanel_;
  }));

  it('should do something', function () {
  });

});
