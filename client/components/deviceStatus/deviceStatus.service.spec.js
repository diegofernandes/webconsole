'use strict';

describe('Service: DeviceStatus', function () {

  // load the service's module
  beforeEach(module('meccanoAdminApp'));

  // instantiate service
  var DeviceStatus;
  beforeEach(inject(function (_DeviceStatus_) {
    DeviceStatus = _DeviceStatus_;
  }));

  it('should do something', function () {
  });

});
